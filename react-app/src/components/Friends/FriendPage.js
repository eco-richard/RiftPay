import React from 'react';
import { NavLink } from 'react-router-dom';
import { useSelector } from 'react-redux';

function FriendPage() {
    return (
        <div>
            <h1>
                THIS IS WHERE DETAILS BY EACH FRIEND GOES
            </h1>
        </div>
    )
}

export default FriendPage;
